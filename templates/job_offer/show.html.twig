{% extends 'base.html.twig' %}

{% block title %}{{ 'company.job_offer.show.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'company.job_offer.show.title'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_offer.id }}</td>
            </tr>
            <tr>
                <th>{{ 'company.job_offer.show.offer.title'|trans }}</th>
                <td>{{ job_offer.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_offer_index') }}">{{ 'company.job_offer.show.actions.back'|trans }}</a>

    <a href="{{ path('job_offer_edit', {'id': job_offer.id}) }}">{{ 'company.job_offer.show.actions.edit'|trans }}</a>

    {{ include('job_offer/_delete_form.html.twig') }}
{% endblock %}
